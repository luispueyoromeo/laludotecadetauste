import type { Metadata } from "next";
import { sabados } from "@/content/actividades";
import { ActivityCta, EmptyState, PageHero } from "@/components/ui";
export const metadata: Metadata = { title:"Sábados ludoteceros", description:"Horarios y programación de los sábados ludoteceros durante el curso escolar." };
export default function Sabados(){return <><PageHero eyebrow="Durante el curso escolar" title="Sábados ludoteceros" icon="calendar"><p>Los sábados son una nueva oportunidad para jugar, compartir y disfrutar en la ludoteca.</p></PageHero><div className="container section"><section className="schedule"><div><p className="eyebrow">Horario confirmado</p><h2>Dos turnos para disfrutar</h2></div><div className="time-cards"><div><span>Mañana</span><strong>9:30 — 13:30</strong></div><div><span>Tarde</span><strong>16:00 — 20:00</strong></div></div></section><section className="calendar-section"><div className="section-heading compact"><div><p className="eyebrow">Próximas fechas</p><h2>Calendario y programación</h2></div><p>Aquí aparecerán las fechas y la actividad preparada para cada sábado.</p></div>{sabados.length===0?<EmptyState title="El próximo calendario está en preparación">Publicaremos las fechas y su programación en cuanto estén disponibles. No necesitas revisar un calendario vacío: también puedes consultarnos directamente.</EmptyState>:<div className="event-list">{sabados.map(s=><article key={`${s.fecha}-${s.titulo}`}><time>{s.fecha}</time><div><h3>{s.titulo}</h3>{s.descripcion&&<p>{s.descripcion}</p>}{s.turno&&<span className="event-shift">Turno de {s.turno}</span>}</div></article>)}</div>}</section><ActivityCta/></div></>}
