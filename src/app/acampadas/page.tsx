import type { Metadata } from "next";
import { acampadas } from "@/content/actividades";
import { ActivityCta, EmptyState, PageHero } from "@/components/ui";
export const metadata:Metadata={title:"Acampadas",description:"Información y próximas acampadas de La Ludoteca de Tauste."};
export default function Acampadas(){return <><PageHero eyebrow="Experiencias para compartir" title="Acampadas" icon="tent"><p>Distintas propuestas a lo largo del año para vivir nuevas experiencias, convivir y disfrutar en grupo.</p></PageHero><div className="container section"><div className="intro-row"><div><p className="eyebrow">Nuevas aventuras</p><h2>Próximas acampadas</h2></div><p>Cada propuesta reunirá aquí sus fechas, programa, información práctica y, cuando dispongamos de ellas, fotografías autorizadas.</p></div>{acampadas.length===0?<EmptyState title="No hay acampadas publicadas en este momento" icon="tent">Estamos preparando este espacio. La próxima propuesta aparecerá aquí con toda la información necesaria y sin confundir actividades pasadas con próximas.</EmptyState>:<div className="camp-list">{acampadas.map(a=><article key={a.id}><span className={`status ${a.estado==="proxima"?"abierta":"pending"}`}><i/>{a.estado==="proxima"?"Próxima":"Realizada"}</span><h3>{a.nombre}</h3><p>{a.descripcion}</p>{a.fechas&&<p><strong>Fechas:</strong> {a.fechas}</p>}{a.programa&&<><h4>Programa</h4><ul>{a.programa.map(x=><li key={x}>{x}</li>)}</ul></>}{a.informacionPractica&&<><h4>Información práctica</h4><ul>{a.informacionPractica.map(x=><li key={x}>{x}</li>)}</ul></>}{a.fotos&&<div className="photo-grid">{a.fotos.map(f=><img key={f.src} src={f.src} alt={f.alt}/>)}</div>}</article>)}</div>}<div className="gallery-placeholder"><div className="gallery-shapes" aria-hidden="true"><i/><i/><i/></div><div><p className="eyebrow">Recuerdos de cada experiencia</p><h2>Un lugar preparado para las imágenes</h2><p>Las galerías se incorporarán cuando haya fotografías propias facilitadas para este uso. Cada imagen tendrá una descripción alternativa y se mostrará dentro de su acampada.</p></div></div><ActivityCta/></div></>}
